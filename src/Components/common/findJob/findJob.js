import React from 'react';
import classes from '../../users/user.module.css';

let FindJob = (props) => {
    return (
        <div> 
        <img src={props.src}/> 
        </div> 
    )
}

export default FindJob;